{% extends "app/base.twig" %}
{% block content %}
    <div class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-12 text-center">
                    <h1 class="m-0 text-dark">{{ lang('LBL_CATEGORY') }}</h1>
                </div>
            </div>
        </div>
    </div>
    <section class="content">
        <div class="container-fluid">
            <div class="card">
                <div class="card-body buton-action-record">
                    <div class="row">
                        <div class="col-sm-12 text-left">
                            <a href="{{ base_url }}/admin/category/create" class="btn btn-info">
                                <i class="far fa-plus"></i> {{ lang('LBL_CREATE') }}
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="card card-info">
                <div class="card-header">
                    <h3 class="card-title">{{ lang('LBL_CATEGORY') }}</h3>
                </div>
                <div class="card-body">
                    <table class="table table-bordered table-hover datatable">
                        <thead>
                        <tr>
                            <th class="text-center">{{ lang(db_def.category.name.label) }}</th>
                            <th class="text-center">{{ lang(db_def.category.for_lang.label) }}</th>
                            <th class="text-center">{{ lang(db_def.category.parent_id.label) }}</th>
                            <th class="text-center">{{ lang(db_def.category.status.label) }}</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for record in records %}
                            <tr>
                                <td><a href="{{ base_url }}/admin/category/detail/{{ record.id }}">{{ record.name }}</a></td>
                                <td>
                                    {% for l in record.for_lang %}
                                        <label class="badge badge-info">{{ config.languages[l] }}</label>
                                    {% endfor %}
                                </td>
                                <td><a href="{{ base_url }}/admin/category/detail/{{ record.parent_id.id }}">{{ record.parent_id.name }}</a></td>
                                <td>{{ lang(db_def.category.status.options)[record.status] }}</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </section>
{% endblock %}